name: MAKO
windows:
  - name: mako
    root: ~/workspace/jive/mako/servicerepo
    panes:
      - vi
  - name: horizon
    root: ~/workspace/jive/ops/horizon
    panes:
      - vi
  - name: horizon-cli
    root: ~/workspace/jive/ops/horizon
